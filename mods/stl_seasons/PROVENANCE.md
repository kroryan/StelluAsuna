# stl_seasons provenance status

This mod was added to StelluAsuna in commit `e1fb3a0` as the project's own
seasonal integration. It exposes the `stellua.get_season()` API and handles
seasonal outdoor water freezing and melting.

The public ContentDB package `tbook/seasons` is a different mod with a
different file and API layout; it is not the source of this directory. The
official Asuna 1.1.5 and Stellua 0.4.4 releases were also checked and contain
no `stl_seasons` directory. This directory does not belong to the upstream
Stellua revision preserved in `LICENSE_STELLUA.txt`.

This is project-owned StelluAsuna integration code and is released under
GPL-3.0-or-later. The full text is in `../../LICENSE`.

SPDX-License-Identifier: GPL-3.0-or-later
