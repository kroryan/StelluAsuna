# stl_seasons provenance status

This mod is present in StelluAsuna from commit `e1fb3a0` and exposes the
`stellua.get_season()` API for seasonal outdoor water freezing and melting.
That commit records when the files entered this repository; it does not prove
where they came from before the repository was created.

The public ContentDB package `tbook/seasons` is a different mod with a
different file and API layout; it is not the source of this directory. The
official Asuna 1.1.5 and Stellua 0.4.4 releases were also checked and contain
no `stl_seasons` directory. This directory does not belong to the upstream
Stellua revision preserved in `LICENSE_STELLUA.txt`.

The project owner has confirmed that this is project-owned StelluAsuna
integration code. It is released under GPL-3.0-or-later. The full text is in
`../../LICENSE`.

SPDX-License-Identifier: GPL-3.0-or-later
